/** Atomically append content to the dated journal and flush to disk. */
export declare function appendJournal(date: string, content: string): void;
/** Read a dated journal (empty string if none). */
export declare function readJournal(date: string): string;
//# sourceMappingURL=journal.d.ts.map